import React from 'react';
import PropTypes from 'prop-types';

RoomNotification.propTypes = {
    
};

function RoomNotification(props) {
    return (
        <div>
            
        </div>
    );
}

export default RoomNotification;