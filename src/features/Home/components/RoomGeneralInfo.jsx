import React from 'react';
import PropTypes from 'prop-types';

RoomGeneralInfo.propTypes = {
    
};

function RoomGeneralInfo({roomDetail}) {
    return (
        <div>
            
        </div>
    );
}

export default RoomGeneralInfo;