import React from 'react';
import PropTypes from 'prop-types';

Inbox.propTypes = {
    
};

function Inbox(props) {
    return (
        <div>
            
        </div>
    );
}

export default Inbox;